<%= form_for(@achievement) do |f| %>
  <% if @achievement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@achievement.errors.count, "error") %> prohibited this achievement from being saved:</h2>

      <ul>
      <% @achievement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :player_id, Player.all.collect { |p| [p.name, p.id] } %><br>
  </div>
  <div class="field">
    <%= f.select :trophy_id, Trophy.all.collect { |p| [p.name, p.id] } %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
